/**
 * POPULAR / TRENDING — owned by Herrick (pairs with Thread).
 *
 * Cross-mode feed sorted by like count or recent activity.
 * GET /api/posts?sort=popular
 */
export default function Popular() {
  return (
    <div>
      <h1>Popular</h1>
      <p style={{ color: 'var(--text-muted)' }}>Build me out</p>
    </div>
  );
}
